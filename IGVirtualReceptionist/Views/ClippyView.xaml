<v:ViewBase x:Class="IGVirtualReceptionist.Views.ClippyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:IGVirtualReceptionist.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <Label x:Name="lblClippyText" DockPanel.Dock="Top" HorizontalAlignment="Center" Content="Wave to Start!">
            <!--<Label.Triggers>
                <Trigger Property="{Binding IsFullScreen}" Value="True">
                    <Setter Property="Content" Value="Wave to Start"/>
                </Trigger>
                <Trigger Property="{Binding IsFullScreen}" Value="False">
                    <Setter Property="Content" Value="Welcome to Infragistics! Select your contact."/>
                </Trigger>
            </Label.Triggers>-->
        </Label>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="Transparent" Background="Transparent"
               Click="Button_Click">
            <Image x:Name="imgClippy" Source="/IGVirtualReceptionist;component/Resources/Images/clippy.png">
            </Image>
        </Button>
    </DockPanel>
</v:ViewBase>
